# Examples of keys etc used in the application.

# It might not be neccesary for a short sample like this, but I prefer being
# generally paranoid about secrets as this makes a slipup when it matters
# that much less likely.

SECRET_KEY = 'uo!ha-ws0tvzjs0@(j0cq=t^6h)v(tseyv(l2n%d1_kg=-+r9w'
